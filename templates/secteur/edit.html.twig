{% extends 'base.html.twig' %}

{% block title %}Edit Secteur{% endblock %}

{% block body %}
<div class="offset-2 col-8">
    <h1 class="text-center">Editer le Secteur</h1>

    {{ include('secteur/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex justify-content-around">
    <a href="{{ path('secteur_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('secteur/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
