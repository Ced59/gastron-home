{% extends 'base.html.twig' %}

{% block title %}CategoriePlats index{% endblock %}

{% block body %}
    <h1>CategoriePlats index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_plat in categorie_plats %}
            <tr>
                <td>{{ categorie_plat.id }}</td>
                <td>{{ categorie_plat.libelle }}</td>
                <td>
                    <a href="{{ path('categorie_plats_show', {'id': categorie_plat.id}) }}">show</a>
                    <a href="{{ path('categorie_plats_edit', {'id': categorie_plat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_plats_new') }}">Create new</a>
{% endblock %}
