{% extends 'base.html.twig' %}

{% block title %}New CategoriePlats{% endblock %}

{% block body %}
    <div class="offset-2 col-8">
        <h1 class="text-center">Ajouter une catégorie de plat</h1>

        {{ include('categorie_plats/_form.html.twig', {'button_label':'Ajouter'}) }}
        <div class="d-flex justify-content-end">
            <a href="{{ path('categorie_plats_index') }}" class="btn btn-danger">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
