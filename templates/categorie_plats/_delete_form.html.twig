<form method="post" action="{{ path('categorie_plats_delete', {'id': categorie_plat.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cet élément?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_plat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
