{% extends 'base.html.twig' %}

{% block title %}CategoriePlats{% endblock %}

{% block body %}
    <div class="offset-2 col-8">
        <h1 class="text-center">CategoriePlats</h1>

        <div class="d-flex justify-content-center">
            <div class="card" style="width: 18rem;">
                <img src="..." class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{ categorie_plat.libelle }}</h5>

                </div>
            </div>
        </div>
        <div class="d-flex justify-content-around mt-4">
            <a href="{{ path('categorie_plats_index') }}" class="btn btn-primary">Retour à la liste</a>

            <a href="{{ path('categorie_plats_edit', {'id': categorie_plat.id}) }}" class="btn btn-primary">Modifier la
                catégorie</a>

            {{ include('categorie_plats/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
