{% extends 'base.html.twig' %}

{% block title %}Edit Plats{% endblock %}

{% block body %}
    <h1 class="mt-4 mb-4 ml-2">Modifier les plats de votre restaurant</h1>

    {{ include('plats/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('plats_index') }}">back to list</a>

    {{ include('plats/_delete_form.html.twig') }}
{% endblock %}
