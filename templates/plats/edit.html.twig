{% extends 'base.html.twig' %}

{% block title %}Modification Plats{% endblock %}

{% block body %}
    <div class="offset-2 col-8">
        <h1 class="mt-4 mb-4 ml-2">Modifier les plats de votre restaurant</h1>
            {{ include('plats/_form.html.twig') }}
            <div class="d-flex justify-content-around">
            <a href="{{ path('plats_index') }}" class="btn btn-primary">Retour à la liste</a>
            {{ include('plats/_delete_form.html.twig') }}
            </div>
        </div>
{% endblock %}
