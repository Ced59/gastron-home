{% extends 'base.html.twig' %}

{% block title %}Plats{% endblock %}

{% block body %}
    <h1>Plats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plat.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ plat.price }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ plat.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plats_index') }}">back to list</a>

    <a href="{{ path('plats_edit', {'id': plat.id}) }}">edit</a>

    {{ include('plats/_delete_form.html.twig') }}
{% endblock %}
