{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau plat{% endblock %}

{% block body %}
    <h1 class="mt-3 text-center">Ajouter un nouveau plat</h1>
    <div class="offset-2 col-8">
        {{ include('plats/_form.html.twig', {'button_label': 'Ajouter'}) }}

        <div class="d-flex justify-content-center mt-3">
            <a href="{{ path('plats_index') }}" class="btn btn-danger">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
