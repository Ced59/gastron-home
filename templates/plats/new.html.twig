{% extends 'base.html.twig' %}

{% block title %}New Plats{% endblock %}

{% block body %}
    <h1>Create new Plats</h1>

    {{ include('plats/_form.html.twig', {'button_label': 'Ajouter'}) }}

    <div class="d-flex justify-content-center">
        <a href="{{ path('plats_index') }}" class="btn btn-danger">Retour à la liste</a>
    </div>

{% endblock %}
