{{ form_start(form, {'attr':{'class':'form-group'}}) }}
<div class="col">
    {{ form_row(form.libelle, {'attr':{'class':'form-control'}}) }}
</div>
<div class="d-flex justify-content-around mt-4">
    <button class="btn btn-primary">{{ button_label|default('Mettre à jour') }}</button>
    <a href="{{ path('categorie_restaurant_index') }}" class="btn btn-danger">Retour à la liste</a>
</div>
{{ form_end(form) }}
