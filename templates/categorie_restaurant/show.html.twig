{% extends 'base.html.twig' %}

{% block title %}CategorieRestaurant{% endblock %}

{% block body %}
    <div class="offset-2 col-8">
        <h1 class="text-center">CategorieRestaurant</h1>

        <div class="d-flex justify-content-center">
            <div class="card" style="width: 18rem;">
                <img src="..." class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{ categorie_restaurant.libelle }}</h5>
                    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the
                        card's content.</p>
                </div>
            </div>
        </div>
        <div class="d-flex justify-content-around mt-4">
            <a href="{{ path('categorie_restaurant_index') }}" class="btn btn-primary">Retour à la liste</a>

            <a href="{{ path('categorie_restaurant_edit', {'id': categorie_restaurant.id}) }}" class="btn btn-success">Editer
                la catégorie</a>

            {{ include('categorie_restaurant/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
